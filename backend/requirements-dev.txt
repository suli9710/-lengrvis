# Compatibility entry point for backend-focused development.  The root lock
# includes the runtime pins plus pytest, coverage, HTTP test tools, linting,
# and the audit tool.  Keep this alias rather than maintaining a second
# un-hashed resolver path that could reintroduce an advisory-fixed pytest.
-r ../requirements-dev-lock.txt
