Repair the previous model output into valid JSON matching the supplied JSON schema.

Return only valid JSON. Do not include Markdown, prose, explanations, code fences, or comments.

JSON schema:
$schema

Failure kind:
$failure_kind

Previous output excerpt:
$output_excerpt
