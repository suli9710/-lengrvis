# Direct dependency set for scripts/install_acceleration.ps1 (supply-chain audit B-3).
#
# Optional acceleration packages are intentionally tracked separately from the
# core backend hash lock. Keep runtime packages aligned with backend/pyproject.toml
# optional extras, and regenerate scripts/acceleration-requirements-lock.txt with:
#
#   uv pip compile --generate-hashes --python-version 3.12 --universal --output-file scripts/acceleration-requirements-lock.txt scripts/acceleration-requirements.txt
#
# The install script reads the section markers below and installs only the common
# packages plus the selected runtime flavor with --require-hashes.

# Common packages
tokenizers==0.22.2
transformers==5.12.1
huggingface_hub==1.21.0
numpy==2.2.6
Pillow==12.2.0

# Runtime: winml
onnxruntime-windowsml==1.27.0.202606181907 ; platform_system == "Windows"
onnxruntime-genai-winml==0.14.1 ; platform_system == "Windows"

# Runtime: directml
onnxruntime-directml==1.22.0 ; platform_system == "Windows"
onnxruntime-genai-directml==0.8.0 ; platform_system == "Windows"

# Runtime: openvino
onnxruntime-openvino==1.22.0
openvino==2025.2.0

# Runtime: cpu
onnxruntime==1.26.0
onnxruntime-genai==0.8.0
