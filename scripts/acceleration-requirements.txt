# Pinned dependency set for scripts/install_acceleration.ps1 (supply-chain audit B-3).
#
# Optional acceleration packages are intentionally tracked separately from the
# core backend hash lock. Keep this file aligned with backend/pyproject.toml
# optional extras and record a release waiver until the optional stack has its
# own hash-locked install path.

# Build toolchain
pip==26.1.2
setuptools==82.0.1
wheel==0.47.0

# Accelerated inference stack
tokenizers==0.23.1
transformers==5.12.1
huggingface_hub==1.20.1
numpy==2.5.0
Pillow==12.2.0
