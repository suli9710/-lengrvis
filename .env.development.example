# Development-only overrides for .env.example.
# Append these values to a local, uncommitted .env when no real LLM provider
# is available. Never use this override for packaged or release builds.
LENGRVIS_MODE=efficiency
LENGRVIS_ALLOW_MOCK_FALLBACK=true
