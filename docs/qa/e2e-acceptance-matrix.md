# Lengrvis End-to-End QA Acceptance Matrix

Last reviewed: 2026-06-08

This matrix defines the release-facing QA coverage for Lengrvis across backend, desktop, and mobile. A release candidate should not ship until every P0/P1 row is either passing with recorded evidence or explicitly waived in the release notes with an owner and follow-up. P2/P3 rows keep optional, platform-specific, and demo-polish risk visible without turning every release into a full-platform certification run.

## Gate Levels

| Level | Meaning | Release expectation |
| --- | --- | --- |
| P0 | Core safety, launch, packaging, or data-protection behavior | Must pass for every release candidate. |
| P1 | Primary user journey or cross-device workflow | Must pass unless the release intentionally excludes that surface. |
| P2 | Important regression coverage for optional or environment-specific capabilities | Run when the feature, platform, or dependency is touched. |
| P3 | Demo polish, observability, documentation, or nice-to-have workflows | Run before stakeholder demos or when touched; track gaps as residual risk. |

## Acceptance Matrix

| ID | Level | Journey | Acceptance criteria | Automated evidence | Manual evidence | Risk if missed |
| --- | --- | --- | --- | --- | --- | --- |
| E2E-001 | P0 | Backend contract and safety baseline | Backend tests complete without failures; policy, audit, approval binding, path sandbox, privacy redaction, provider fallback, task lifecycle, and WebSocket contracts remain green. | `python -m pytest backend/tests -q --maxfail=1` or `.\scripts\run_tests.ps1` | Review first failure if any; no release with an unresolved safety or data-protection failure. | Safety/data regression ships, or core API behavior breaks. |
| E2E-002 | P0 | Desktop TypeScript/API contract | Renderer, main, preload, shared IPC types, and API client compile without TypeScript errors. | `npm --prefix desktop run typecheck` | Confirm no local-only generated files were required to make typecheck pass. | Desktop shell may fail at startup or call stale backend contracts. |
| E2E-003 | P0 | Mobile TypeScript/API contract | Companion app screens, API client, token storage, notification, and remote-input grant paths compile without TypeScript errors. | `npm --prefix mobile run typecheck` | Confirm Expo/native dependency drift is documented if typecheck is blocked by install issues. | Mobile approvals or pairing may fail silently after release. |
| E2E-004 | P0 | Mobile token protection | Mobile WebSocket token is passed through the subprotocol flow and avoids URL logging. | `npm --prefix mobile run smoke:token` | Inspect logs only when smoke fails; never paste real tokens into artifacts. | Tokens could leak through logs, URLs, screenshots, or proxy traces. |
| E2E-005 | P0 | Desktop IPC and backend launch security | Desktop preload/main IPC allows only expected channels; bundled backend environment keeps local-service assumptions intact. | `npm --prefix desktop run smoke:ipc` | Check failures for any expansion of privileged IPC or backend env leakage. | Privileged desktop surface or backend assumptions expand unnoticed. |
| E2E-006 | P0 | Document scope and remote input approval boundaries | Document scope grants and remote input grants are bound, scoped, and reject mismatches. | `npm --prefix desktop run smoke:document-scope`; `npm --prefix desktop run smoke:remote-input-grant`; `npm --prefix mobile run smoke:remote-input-grant` | Manually confirm any new approval wording still distinguishes read-only, reversible, destructive, and forbidden actions. | Cross-scope access or remote input could bypass the intended consent boundary. |
| E2E-007 | P1 | User launches desktop and submits a task | Desktop starts, connects to `http://127.0.0.1:8000`, submits a natural-language task, shows agent progress, and surfaces completion/error state. | `.\scripts\run_tests.ps1`; desktop build/typecheck | Start with `.\scripts\start_app.ps1` or `npm --prefix desktop run dev`; verify first-screen task entry, progress timeline, and safe error copy. | The main product loop may look broken even when backend tests pass. |
| E2E-008 | P1 | Safety review and approval loop | R2/R3 actions produce dry-run previews and approval records; R4 actions are blocked; audit log records the decision path with redaction. | Backend policy/audit tests under `backend/tests`; `npm --prefix desktop run smoke:ipc` | Submit one reversible file action and one forbidden credential/token request; verify expected approval/blocking UX. | Users may lose trust in high-risk action handling. |
| E2E-009 | P1 | File and document workflows | Authorized file search, duplicate detection, document extraction, document QA citations, summary/report fallback, and OCR paths behave without exposing out-of-scope files. | Backend file/document/privacy tests under `backend/tests`; `npm --prefix desktop run smoke:browser-activity` for UI-adjacent document flows | Use a local test folder with PDF/DOCX/XLSX/PPTX/CSV and verify cited answers name the right source. | A marquee productivity path may return wrong, uncited, or out-of-scope answers. |
| E2E-010 | P1 | Mobile pairing and approval companion | Pairing code exchanges for a mobile JWT; pending approvals list loads; approve/reject decisions round-trip to desktop/backend; remote screen view remains read-only unless explicit remote input approval is granted. | `python -m pytest backend/tests/test_mobile_pairing.py backend/tests/test_lan_api_guard.py backend/tests/test_remote_desktop.py -q`; `npm --prefix mobile run smoke:token`; `npm --prefix mobile run smoke:remote-input-grant` | Run backend on LAN with `.\scripts\start_app.ps1 -BackendHost 0.0.0.0`; pair a device or emulator and approve/reject one request; record whether the LAN run is HTTP dev/test-only or HTTPS/WSS with an explicit device certificate trust path. | Cross-device demo or user approval flow may fail outside localhost, or LAN transport claims may outrun verified trust evidence. |
| E2E-011 | P1 | Browser read/write automation | Read-only browser tools can inspect pages; mutating actions require dry-run and approval binding; credential, payment, one-time-code, and token entry remain blocked. | Backend browser/policy tests; `npm --prefix desktop run smoke:browser-activity` | Try a benign form fill with dry-run, then attempt a credential-like input and confirm policy blocks it. | Web automation could perform risky actions without clear consent. |
| E2E-012 | P0 | Windows release artifacts | Backend executable, portable directory, portable zip, self-extracting executable, app resources, and backend resources exist and are non-empty; backend and launcher artifacts have valid PE headers and release-sized files; backend executables pass runnable smoke by short CLI exit or isolated `/health` probe; portable launcher is preflighted without automatically opening the GUI. | `npm run release:check` for structural verification; `npm run release:smoke` or `.\scripts\build_all.ps1 -VerifyOnly -RunExecutableSmoke -SmokeTimeoutSeconds 45` for runnable release smoke; attach `.tmp\packaging-smoke` diagnostics if it fails or times out. | Launch the portable artifact from a clean directory only during manual sign-off, then submit a read-only diagnostic task and record artifact paths, candidate commit, and whether GUI launch reached first screen. | A release artifact may be present but not executable, uninstallable, incomplete, or missing the backend. |
| E2E-013 | P2 | Local LLM and privacy mode | Privacy mode does not silently fall back to `MockProvider`; configured Ollama/LM Studio/llama.cpp-compatible server health is visible and failures are explicit. | Backend provider/settings tests; optional Ollama packaging smoke when release includes bundled resources | Toggle privacy mode with and without a local LLM server and verify the status/error copy. | Privacy-mode claims may overstate what the candidate can actually do. |
| E2E-014 | P2 | Hardware acceleration settings | ONNX provider settings save, status/warmup endpoints respond, and unsupported hardware degrades with clear diagnostics. | Backend ONNX/provider tests | Run `.\scripts\install_acceleration.ps1 -Runtime auto -SkipModels` on a representative Windows machine when acceleration changes. | Acceleration changes may create confusing setup failures on real devices. |
| E2E-015 | P2 | macOS packaging | macOS backend binary is built on macOS for the target arch; DMG includes the matching backend resource. | `bash scripts/build_backend_mac.sh <arch>`; `npm --prefix desktop run dist:mac:<arch>` | Install DMG on the target arch and verify backend health plus one read-only task. | Non-Windows release claims may ship with missing or mismatched backend resources. |
| E2E-016 | P3 | Demo script and screenshots | The current demo script covers first launch, one read-only task, one approval, one blocked risky request, and one document answer without exposing private user data. | `npm run qa:gate` when demo is release-bound; otherwise record latest passing smoke commands. | Run the demo script once in a clean profile and capture sanitized screenshots or notes. | Stakeholder demo may drift from the verified product path or leak local data. |
| E2E-017 | P3 | User-facing copy, logs, and handoff notes | Error copy, release notes, and QA handoff use current product terms and do not claim unsupported local model, mobile control, or packaging capabilities. | Not automated; optionally run docs lint if introduced later. | Review README/release notes/demo notes against this matrix and record any intentional exclusions. | The release may overpromise capability or leave support without known limitations. |
| E2E-018 | P1 | Local and hybrid model evidence | Settings model boundary profile shows quick/privacy/hybrid mode, recommended model, estimated model size, hardware state, speed estimate, failure repair action, and confirms privacy failures do not auto-fall back to cloud. | Backend provider/settings tests; Settings hardware/local model smoke when available. | Toggle quick/privacy/hybrid on a clean profile and record local model readiness or the exact blocked reason. | Local/privacy positioning may be claimed without an actual usable or explainable local path. |
| E2E-019 | P1 | Skill/App integration sample | A non-private Skill sample imports or displays successfully and its Product Manifest cards identify file read/write, UI control, network, messaging, delete, preview, and rollback/handoff boundaries. | `python -m pytest backend/tests/test_app_skill_protocol.py backend/tests/test_app_skill_packages.py backend/tests/test_skill_loader.py -q`; desktop typecheck for manifest UI; tests must cover manifest permissions, schema path, smoke metadata, zip import validation, and repository samples no longer falling back to `legacy.unspecified`. | Import one migrated non-private directory or zip Skill sample; record sample id/source, import path, and screenshots or notes confirming Product Manifest cards match the manifest intent. | Skill ecosystem claims may hide risky permissions, leave sample migration incomplete, or fail before real users can evaluate the model. |
| E2E-020 | P3 | Template demo path evidence | At least one task template demo path uses disposable data, has expected screen/result notes, and records fallback or residual risk when a step is not run. | `npm run qa:gate` when release-bound; latest smoke evidence may be referenced for demos. | Run one 60-second, 3-minute, or 10-minute path from `docs/demo-script.md` and record which template, data set, and outcome were used. | Platform packaging may look like a capability list instead of a reproducible product story. |

## Evidence Checklist

- Record the exact command, date, commit, platform, and result for each automated gate.
- Attach screenshots or short notes only for manual P1/P2/P3 checks that are not already covered by automated logs.
- Record local model readiness/smoke, Skill sample, mobile companion, document citation, and template demo evidence whenever those capabilities are mentioned in demo material or release notes.
- For Windows release candidates, record both structural packaging verification and `release:smoke` or `build_all -VerifyOnly -RunExecutableSmoke`; attach `.tmp\packaging-smoke` logs only when the smoke fails or times out.
- For mobile/LAN claims, record the scheme, host, and explicit certificate trust path; HTTP LAN remains dev/test-only evidence, not production TLS readiness.
- Treat missing dependencies, absent artifacts, or skipped platform checks as "blocked", not "passed".
- Any waiver must include an owner, user impact, expiry condition, and a follow-up issue or task.
- P0 failures are stop-ship; P1 failures require an explicit release exclusion or waiver; P2/P3 failures become residual risks unless the touched feature or demo depends on them.
