# Lengrvis Agentic Product Evals

Last reviewed: 2026-06-09

This file tracks the eval-first operating contract for the multi-agent productization push. The goal is to make Lengrvis feel safe, capable, and ready for a first-time user without turning the UI into a developer console.

## Product Bar

Lengrvis should be judged against OS-agent products such as Marvis, Copilot-style local assistants, ChatGPT Agent/Operator, and self-hosted agent workbenches. It does not need to copy their surface area; it needs to win on a clear lane:

- Local-first OS agent that can finish useful computer tasks.
- Safety review, dry-run approval, rollback, and audit trail for state-changing work.
- Simple first-run experience for non-technical users.
- Mobile companion flows that do not require users to understand IP addresses, ports, or WebSocket details.
- Repeatable release evidence instead of demo-only confidence.

## First-Round Completion Criteria

The first productization round is complete only when these checks are true or explicitly waived in the QA handoff:

| Area | Eval | Pass signal |
| --- | --- | --- |
| First win | A fresh user can start one local read-only task from the first screen. | Task reaches visible progress and either completes or fails with actionable next steps. |
| Behavior smoke | Mobile pairing and remote-input smoke prove real request/response behavior. | Smoke scripts exercise HTTP/WS contracts or a self-contained local behavior stub, not only source regex checks. |
| Safe actions | R2/R3 actions require preview and approval; R4 stays blocked. | Backend tests and demo gate include one approved reversible action and one blocked forbidden request. |
| Task evidence privacy | Task recording is opt-in, local-only, and replay/timeline stay redacted by default. | Public task evidence returns summaries, counts, and labels only; raw screenshots, file names, tool args/results, hidden prompts, and review reasons stay out of timeline/replay/support exports. |
| Mobile pairing | Pairing supports built-in QR scan, paste, and manual payload paths with classified failures. | Source/auto smoke proves the scanner entry exists; real-device evidence is still required before scan-to-pair release claims. |
| Diagnostics | Desktop exposes installed version, backend version/status, local-only refresh, log locations, and exportable diagnostics. | A user can refresh local status, find logs, and produce a redacted support package without treating it as public-safe or as an online updater. |
| Release readiness | Release gate proves runnable artifacts. | `npm run release:check` or an equivalent command runs executable smoke, not only file existence checks. |

## Current Evidence Snapshot

Last synchronized: 2026-06-09, local dirty worktree. Treat these as handoff evidence for the current workspace, not as reusable release-candidate sign-off.

Evidence labels used in this snapshot:

- Dev smoke/typecheck/unit evidence means commands run in the current dirty workspace. It supports merge confidence and regression triage only.
- Packaged evidence means a built artifact was launched or inspected, such as `npm run smoke:portable-first-screen`; it still needs commit/build identity and candidate context before release use.
- Clean-machine evidence means a fresh machine/profile install or explicitly blocked handoff with runtime/model/version details. Current local-model helpers are templates unless a real install/start/pull smoke is recorded.
- Real-device evidence means phone/emulator camera, LAN/WSS, certificate trust, screenshots, and artifact review from the target device path.
- RC sign-off means the candidate commit/build id, exact commands, platform, manual P1 checks, waivers, and residual risks are all recorded together.

| Area | Current status | Evidence | Boundary |
| --- | --- | --- | --- |
| Mobile behavior smoke | Passing | 2026-06-09 targeted reruns passed: `npm --prefix mobile run smoke:token`, `npm --prefix mobile run smoke:task-companion`, and `npm --prefix mobile run smoke:remote-input-grant`. The scripts use `mobile/scripts/behavior-smoke-helpers.cjs` to exercise local HTTP requests and WebSocket handshakes; token smoke proves non-loopback HTTP LAN cannot pair, restore old persisted sessions, call token-bearing APIs, or construct mobile WebSocket connections, and now also proves an `https://` session whose backend metadata says TLS is disabled / websocket is `ws` is blocked instead of being labeled safe. PairScreen source assertions keep parsed QR transport metadata active across manual-entry toggles and require submit-time `describeBaseUrlSecurity(..., payload.security)` fail-closed before any pairing request. Task-companion smoke proves list/start/follow-up/pause/resume/cancel client contracts, privacy-title handling, and strict completed-result copy only when `completion_evidence.level=completed_result`, `result_verified=true`, and `signoff=false`. Backend mobile tests also prove Companion tasks are bound to the creating paired device before list, detail, follow-up, pause, resume, or cancel. `mobile-token-smoke` also asserts the PairScreen camera entry and native camera permission config at source/config level. | This proves client contract behavior against a local stub plus backend API authorization, and proves the built-in scanner code/config path exists. It is not a real phone, real camera QR scan, LAN TLS trust, router/firewall path, or React Native UI automation click path. |
| Mobile pairing QR/payload | Automated QR image + behavior/source smoke | `desktop/scripts/mobile-pairing-qr-smoke.cjs` generates a real PNG QR data URL from the desktop payload and verifies the Settings source contains the QR image render path; `mobile/scripts/mobile-token-smoke.cjs` parses JSON, `lengrvis://pair` URL, and pasted text payloads, performs `POST /api/pair/confirm` and token-subprotocol WebSocket checks, and asserts that PairScreen imports `expo-camera`, renders `CameraView` with QR-only `onBarcodeScanned`, uses camera permissions, exposes the "打开相机扫码" entry, keeps paste fallback copy, and declares native camera permission/config. Backend LAN transport tests cover HTTPS/WSS metadata for ready and misconfigured certificate-file states. | This is QR payload/image generation, parser/client behavior, PairScreen source/native-config assertion, and LAN TLS metadata evidence. It proves an in-app scan code path exists, but real phone/emulator camera scan, LAN router/firewall path, an actual HTTPS/WSS serving path, and explicit device certificate trust remain manual evidence. |
| Mobile approval/task redaction | Backend mobile+remote targeted suite passing | `python -m pytest backend\tests\test_mobile_pairing.py backend\tests\test_remote_desktop.py -q` passed with latest combined `132 passed`. The suite covers mobile approval list/detail/WebSocket created-event payload redaction, nested `model_action` args, local paths in approval text fields, selectors, tokens, values, support-only details, mobile token scope, paired-device binding, companion task boundaries, strict mobile task `completion_evidence` summary, LAN TLS metadata, and remote WS auth/error boundaries before they reach phone-facing evidence. | This is backend API/WebSocket payload and metadata evidence, not real phone approval screenshots, lockscreen/background evidence, proxy-trace review, or LAN/WSS artifact sign-off. Keep run-specific artifact redaction review in the mobile matrix. |
| Remote input grants and WS errors | Covered by behavior smoke plus targeted backend redaction tests | `mobile/scripts/remote-input-grant-smoke.cjs` covers grant token claim, `/ws/remote/input` subprotocol, wrong token rejection, revoke, expiry, active-grant mismatch blocking, and now source-asserts beginner-facing RemoteScreen copy: only-read viewing, authorized input, grant remaining/ended/expired states, and no protocol/host/token/certificate internals in the transport notice. Current remote WS implementation returns generalized client error code/message for screen control, capture failure, input rejection, and unexpected input failure, while raw exceptions are recorded only as redacted audit evidence. 2026-06-09 targeted integration passed: `npm --prefix mobile run typecheck`, `npm --prefix mobile run smoke:remote-input-grant`, `npm --prefix mobile run smoke:token`, `npm --prefix mobile run smoke:task-companion`, desktop typecheck, desktop mobile-pairing QR smoke, desktop remote-input grant smoke, and backend mobile+remote targeted combined `132 passed`, covering auth/scope, remote-view versus remote-input token separation, query-token rejection, revoke/expiry/disable close behavior, invalid screen control, screen capture failure, unsupported input, policy/tool rejection, remote input unexpected exception redaction, click/text/key input approval contracts, and active remote-input approval matching the current mobile binding. Shared evidence should cite the HMAC `binding_ref` or a redacted active-grant label; raw `deviceId`/`grantId` correlation stays local-only. Scheduler/preflight counts require an exact command/log before citation. | Does not replace a real mobile LAN/WSS remote-screen session, weak-network evidence, lockscreen/background evidence, or device-side screenshots proving safe copy and grant matching. The 2026-06-09 mobile smoke still includes source assertions/client behavior, not a React Native rendered-device screenshot. Do not use raw exception text, selectors, hosts, local paths, tokens, or device identifiers in client-facing remote errors. |
| Task recording and replay privacy | Code/test boundary in current worktree | `backend/tests/test_task_recordings.py` covers opt-in step screenshots, SQLite BLOB storage, timeline recording redaction, explicit file-name image route, and perception capture not writing task recordings. `backend/tests/test_tasks_replay.py` covers public-safe timeline/replay/task-list/agent-message/review/progress/explain payloads: tool args/results are redacted, recording URLs/file names/recording ids are omitted, review reasons are counted but not returned, bare file names and hidden/developer prompt prose are redacted from public task text, and `raw_redacted=true` is set on replay. | This is backend public-API evidence, not packaged Electron replay UX, mobile task evidence UX, real-device screenshot/recording evidence, or a public-safe diagnostics-package guarantee. Task recording remains explicit opt-in; demo or support artifacts must be collected on disposable data unless separately reviewed. |
| Diagnostics and product metrics | Passing with local refresh, task-recording status, and export redaction boundary | `python -m pytest backend\tests\test_system_diagnostics.py -q` passed with 8 tests. Local diagnostics include `product.version`, `update_channel.status=not_configured`, `check_action=refresh_local_status`, local release notes, data/database/log paths, audit verification, LAN readiness, task recording default-policy/status, local model readiness, anonymous `product_metrics`, and `product_funnel`. Diagnostics export writes a redacted support package under `<data_dir>\diagnostic-packages` with `support_package_redaction`, path labels for data/database/log directories and release notes, process username labels, and checks that secrets, task bodies, tool/approval sensitive text, device names, grant ids, pairing codes, model paths, raw local paths, organization-like folders, usernames, task recording images, recording file names, and recording paths do not appear in the package. 2026-06-09 `npm --prefix desktop run smoke:system-diagnostics-ui` passed after tightening beginner copy: export must be manually clicked, the app does not auto-send diagnostics, user-visible copy avoids raw `public_safe` terms, and the saved-path area shows only filename/compact path by default while the reveal button keeps the actual local path for same-machine opening. | This is local diagnostic/export evidence and Vite-preview UI evidence, not cloud telemetry, long-term analytics, complete online auto-update, a complete crash/update support pipeline, clean-machine RC sign-off, packaged-app support-bundle sign-off, or public-safe diagnostics evidence. Local UI may still show some same-machine troubleshooting paths such as log locations; the exported support package is redacted for the tested fields but still should not be described as broadly public-safe or publishable. |
| Release readiness | Development evidence passing; RC unproven | Strict `npm run release:check` completed with exit 0 on 2026-06-08, including `release:safety`, structural packaging checks, portable directory/zip source-map checks, and backend/portable backend `/health` smoke. After later hardening tests were added, an earlier `npm run qa:gate` completed with exit 0 and reported `1337 passed, 1 skipped` plus desktop/mobile typecheck and smoke. Current targeted development evidence also records desktop typecheck, mobile typecheck, mobile token/task-companion/remote-input smokes, desktop mobile-pairing QR and remote-input grant smokes, backend mobile+remote targeted combined `132 passed`, fixed first-launch smoke passing, prior P1/P2 review findings closed, and this round's public task text, realtime malformed-message, mobile QR transport metadata, and remote-input active-grant mismatch findings closed at contract/source-smoke level. Scheduler/preflight counts require exact command/log evidence before they are cited. | This is current-worktree development evidence. A release-candidate handoff still needs the exact commit/build id and a fresh `release:check` run on that stable artifact, plus real-device LAN/WSS evidence for mobile claims. |
| Workspace formatting hygiene | Passing with CRLF warnings only | 2026-06-09 `git diff --check` exited 0 and emitted only Git LF-to-CRLF working-copy conversion warnings. | This is whitespace hygiene evidence only. It is not runtime, packaged, clean-machine, real-device, or RC evidence. |
| Desktop renderer first launch | Fixed and passing on current dirty workspace | 2026-06-09 `npm --prefix desktop run smoke:first-launch` passed. It builds the renderer, launches Vite preview with disposable Chromium profiles, verifies the first visible template is the novice-safe `检查电脑状态` entry, confirms no-input/read-only/no-cloud/no-change/30-second copy, clicks it through to the system-check view, checks the Home 成果区 recent-result/fallback and Task Workspace read-only boundary, verifies completed-but-unverified task evidence stays actionable instead of claiming a verified result, verifies malformed realtime message notices use safe summaries instead of raw previews, verifies the Home trust card names file-content upload and dangerous-action approval, and aborts backend requests in a second profile to assert the offline connection guidance and disabled send action. | This proves renderer preview behavior against a stubbed/unavailable backend plus smoke/mock template evidence. It does not replace packaged Electron main/preload lifecycle coverage or a full GUI-launched portable task. |
| Privacy/offline first-screen task | Passing with Settings visual regression | `python -m pytest backend\tests\test_privacy_mode_offline_eval.py -q` passed with `12 passed`. It proves privacy mode blocks cloud/mock fallback when no local LLM is reachable, returns local-model setup guidance, keeps file search local, exposes local-only AI status evidence with redacted provider errors, lets the first-screen `检查电脑状态` task complete through deterministic `system.diagnostics`, and uses deterministic local document summary/QA fallback with citations before any local LLM is installed. Ollama backend tests now record `53 passed` for status/setup-plan/install/start/pull/install-local-model contracts. 2026-06-09 `npm --prefix desktop run smoke:settings-local-model` passed after the Settings page was made more novice-facing: visible copy says "本地 AI 应用/服务" instead of provider/internal contract text, forbids English/internal terms in the rendered page, only claims paths hidden when `verification.paths_redacted=true`, only claims verified local AI when `verification.ready=true`, and adds a negative case proving Chinese backend errors containing a model path, URL, and token sentinel do not render. It still verifies quick/privacy/hybrid copy, recommended model, size, hardware, speed estimate, repair action, one-click setup affordance, no silent cloud fallback, and responsive layout constraints at 1366px and 900px widths, writing the same `.tmp\qa-evidence\settings-local-model-experience-smoke-*.png` screenshots. | This is backend/API evidence plus Vite-preview, mocked-data Settings visual regression evidence. It does not prove a bundled offline model, a clean-machine packaged install, a real local-model install/start/pull smoke, a GUI-launched local-model document task, or packaged release-candidate visual sign-off. |
| Desktop token/preload boundary | Passing | `npm --prefix desktop run smoke:preload-api`, `npm --prefix desktop run smoke:ipc`, and `npm --prefix desktop run smoke:desktop-ws` passed on 2026-06-08. These cover preload JSON sanitization, privileged IPC channel checks, desktop token loopback-only HTTP proxy/runtime mode, notification WebSocket, BrowserHost bridge, and realtime WebSocket subprotocol transport. | This proves local bridge contracts and token routing in smoke harnesses; it does not replace a full external pentest or packaged-app malicious renderer exercise. |
| Portable launcher/backend/renderer diagnostics | Passing read-only entry and natural-language submission/task evidence | Latest `npm run smoke:portable-first-screen` evidence at `.tmp\portable-first-screen-smoke\run-20260608-154045-41396-6013e259\portable.status.log` launched `dist\Lengrvis-win-portable\Lengrvis.exe` with temporary state/data/config dirs, observed a visible portable window process, verified `/health`, proved token-authenticated local-only diagnostics, connected to packaged renderer CDP with Playwright, clicked the read-only `检查电脑状态` entry, observed packaged renderer `/api/system/diagnostics` plus read-only diagnostics copy, and verified `tasks=0`, `runs=0`, `chat messages=0`, and `diagnostic-packages=0` after that read-only click. The same run submitted `帮我检查这台电脑` through the packaged command dock, observed `POST /api/runs`, recorded backend read-only/system diagnostics task evidence `task_99963aecac4841d2af25feb2f675c2ad` with `tasks=1`, `runs=1`, `chat messages=0`, and `diagnostic-packages=0`, and records explain `completion_evidence.level` plus `result_verified` for that boundary. | This is separate from `release:check`. Missing artifacts are `[blocked]` exit 2, and unavailable renderer DOM automation is `[unsupported]` exit 2 unless explicitly rerun with `-AllowBackendOnlyPass` for legacy backend-only evidence. Submission plus backend read-only/system diagnostics task evidence does not prove clean-machine candidate install, full task-result completion, result quality sign-off, real-device validation, or platform distribution sign-off. API explain `completion_evidence` may be called completed-result evidence only when `completion_evidence.level=completed_result` and `result_verified=true`; that remains result evidence, not result quality or release sign-off, and more specifically not result quality review, RC sign-off, or release sign-off. `completion_evidence.signoff` remains false. |
| Developer Engine write boundary | Direct write-capable tools blocked | `backend/tests/test_claude_code_config.py` rejects `Edit`, `Write`, unsafe Bash, and `Agent` allowlist entries; `backend/tests/test_execution_engines.py` proves unsafe Developer Engine allowlists fail before launching Claude Code. | This is a read-only Developer Engine boundary, not an approved write-capable Developer Engine path. Future write support needs explicit approval binding and sub-agent inheritance tests. |
| Skill/App Product Manifest | P1 evidence strengthened with repo-sample real import and DOM labeling boundary | `product-manifest-showcase` under `test_data/skills` is a non-private repo sample whose manifest declares file read/write/delete, UI control, external network, messaging, dry-run preview, smoke metadata, and rollback/handoff copy. `python -m pytest backend/tests/test_skill_routes.py -q` imports that real directory through `/api/skills/import`, then calls `/api/skills` and verifies the installed catalog permissions, R3 risk, installed root/manifest path, dry-run flag, authorized-path flag, rollback/handoff copy, and no `legacy.unspecified` fallback; it also reloads the catalog-referenced installed manifest to prove smoke metadata survived the import copy. Backend package tests verify the same permissions flow into runtime capabilities. `npm --prefix desktop run smoke:skill-manifest-ui` builds the renderer, opens Vite/Chromium, mocks `/api/skills`, verifies rendered manifest/source labels, and writes `.tmp\qa-evidence\skill-manifest-ui-smoke.png`. | This proves one repo sample through a real local import and catalog route plus mocked DOM render evidence. `/api/skills` still does not expose `smoke_tests` directly; the route test verifies smoke metadata via the installed manifest path returned by the catalog. It is not a signed public plugin marketplace, not a broad third-party sample ecosystem, not an external release-candidate zip/directory, and not proof that inferred signals are authoritative permissions. |

## Active Agent Roster

Default productization runs use six parallel agents: four development workers, one testing worker, and one review worker. Treat this table as dispatch evidence only; completion still requires merged code, passing verification, and review of the current worktree.

| Agent | Role | Owned scope | Done condition |
| --- | --- | --- | --- |
| Tesla | Dev 1 | Desktop Settings local-model setup UX | Settings explains the local AI setup path in beginner-facing Chinese, hides provider/internal contract text, and fail-closes on missing verification or sensitive backend text. |
| Nietzsche | Dev 2 | Desktop System Info diagnostics export UX | Diagnostics export copy is beginner-facing, fail-closed, manually triggered, and does not display the full saved support-bundle path by default. |
| Epicurus | Dev 3 | Desktop first-screen task entry | The first visible task template is a no-input read-only computer check with local/no-cloud/no-change boundaries and trust-card evidence. |
| Singer | Dev 4 | Mobile remote-screen/input state UX | Mobile RemoteScreen explains read-only versus authorized input states and hides protocol/host/token/certificate internals from beginner-facing copy. |
| Fermat | Test | Targeted desktop/mobile verification commands | Fast gates and targeted regressions are run or explicitly reported as blocked with command-level evidence. |
| Copernicus | Review | Security, privacy, beginner UX, and evidence-boundary review | Findings are concrete, severity-ordered, and tied to file/line references or clearly marked as residual manual evidence. |

## Review Rules

- Do not count a capability as done because a code path exists; count it when a user-facing flow or automated gate proves it.
- Keep UI controls novice-friendly: one primary action, clear status, and failure text that tells the user what to do next.
- Treat safety and privacy regressions as stop-ship even when the product demo looks good.
- Prefer small, independently verifiable changes over broad refactors.
- Record any waiver with owner, reason, expiry condition, and follow-up task.

## Active P1 Gaps

- Natural-language task result quality and Task Workspace: current packaged evidence proves command-dock submission plus backend read-only/system-diagnostics task evidence; it does not prove a high-quality user-facing result, durable Task Workspace artifact, next-step affordance, or result-quality sign-off.
- Local-model clean-machine validation: current Settings smoke and Ollama backend tests prove UI/contract behavior; they do not prove clean-machine packaged install/start/pull, bundled/offline model readiness, or a real local-model document task.
- Mobile real-device LAN/TLS: current behavior smokes, QR payload/source assertions, and backend LAN TLS metadata prove prerequisites; they do not prove real phone/emulator camera pairing, router/firewall traversal, WSS serving, device certificate trust, or redacted real-device screenshots.

## Next Evals To Add

- Expand `desktop/scripts/first-launch-smoke.cjs` once a stable packaged Electron automation hook exists: cover main/preload backend lifecycle wiring in addition to the current disposable Vite preview renderer coverage.
- Expand the current PairScreen source/config assertions in `mobile/scripts/mobile-token-smoke.cjs`, plus `desktop/scripts/mobile-pairing-qr-smoke.cjs` and `mobile/scripts/mobile-task-companion-smoke.cjs`, with a real camera/emulator scan, HTTPS/WSS LAN trust evidence, and React Native click-path fixture when a stable test device path exists.
- Expand mobile approval redaction evidence beyond backend payload tests: capture real phone/emulator approval list/detail/WebSocket update screenshots and proxy/log notes showing nested args, local paths, selectors, tokens, and support-only details stay redacted.
- Expand remote WS evidence beyond backend branch tests: verify real phone/emulator WSS error states, weak network, lockscreen/background behavior, and screenshots/log notes showing beginner-safe copy while audit/log evidence keeps only redacted diagnostics.
- Expand task recording evidence beyond backend public API: verify packaged Electron timeline/replay controls show only summaries by default, require explicit opt-in before collecting screenshots, and keep mobile task views redacted on a real phone/emulator.
- Expand `backend/tests/test_privacy_mode_offline_eval.py` through a GUI-backed document picker/task once a stable automation hook exists; current backend coverage already proves no cloud/mock fallback, setup guidance, local file search, local-only status, offline first-screen system diagnostics, and deterministic document summary/QA fallback.
- Expand diagnostics export coverage beyond the current seeded secrets, cookies, task/device/grant/model-path/local-path/user redaction, organization-like folder names, and log snippets: add broader real log shapes and any future crash/update artifacts before describing a support package as shareable outside a controlled support context. Local UI may still show the generated package save path so users can find it.
- Expand Skill Product Manifest evidence beyond the current repo-sample import: expose smoke metadata directly in `/api/skills` or add a release-candidate non-private zip/directory import, then keep screenshots/notes showing manifest-declared permissions separately from inferred risk signals.
- Expand the current packaged natural-language GUI evidence beyond submission/task evidence: after the read-only entry pass, prove `帮我检查这台电脑` creates visible progress plus result artifacts without write side effects, and capture explain `completion_evidence.level`/`result_verified` when claiming completed-result evidence. Only `completion_evidence.level=completed_result` with `result_verified=true` may be called completed-result evidence, and that still is not result quality review, RC sign-off, or release sign-off.
- Expand Settings local-model visual regression beyond the current Vite/mock 1366px and 900px desktop slice when a stable packaged Settings automation path exists.
- If a write-capable Developer Engine is ever enabled, add approval-regression tests where `Edit`/`Write` and Claude Code sub-agent paths require explicit approval binding, reject forged `approved`/`approval_id` fields, and leave a redacted audit event. Current policy keeps these paths disabled.
